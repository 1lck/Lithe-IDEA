#pragma once

#include <QWidget>

namespace lithe::windows {

// Aligns the native Windows frame with the dark Qt workbench palette.
void applyWindowsWindowChrome(QWidget& window);

}  // namespace lithe::windows
