# root comment
[server.production]
host = "example.test" # inline comment
port 8443 ; port comment
enabled = on
hexadecimal = 0xCAFE
path = "/tmp/lithe # remains a string"
include "fragment.conf";
nested.key = release#42
worker_processes 4
; legacy comment
