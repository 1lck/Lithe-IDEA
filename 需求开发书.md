# Lithe-IDEA 需求开发书

## 1. 项目概述

### 1.1 项目名称

- 项目名：Lithe-IDEA
- 中文名：轻舟
- 产品定位：轻量、专业、IDEA 风格的代码浏览、运行、调试与版本管理工作台

### 1.2 一句话定义

> 保留 IntelliJ IDEA 优秀的工作台布局、信息架构和开发习惯，使用更轻量的底层实现，专注于代码理解、变更审查、项目运行、调试和 Git 管理。

### 1.3 项目背景

在 AI 编程工具普及后，代码生成和重复编辑不再是 IDE 的唯一中心。开发者仍然需要一个可靠的本地工具来：

- 浏览项目结构和代码
- 查看外部工具产生的文件变化
- 审查 Diff 和 Git 状态
- 启动、停止和调试项目
- 查看测试、日志和错误
- 使用代码导航、语法检查和重构能力

Lithe-IDEA 不负责 AI 模型调用、Agent 会话或聊天功能。外部工具可以修改项目目录，Lithe-IDEA 负责及时观察、验证和管理这些变化。

### 1.4 当前开发策略

第一版先研究 `intellij-community-master` 中的 IntelliJ UI 实现，提取 Project、Editor、ToolWindow、Run/Debug 和 Welcome Screen 的页面结构、尺寸关系、交互和状态组织方式，再用独立的轻量 UI 底座重做页面空壳。当前不编译 IntelliJ Platform 作为产品，不直接把 Swing/JVM UI 搬进最终应用，也不接入 AI。

## 2. 产品目标

### 2.1 必须实现

1. 提供接近 IntelliJ IDEA 的成熟工作台布局和操作习惯。
2. 支持打开本地项目并浏览、编辑和搜索代码。
3. 支持语法高亮、语法检查、类型诊断和代码导航。
4. 支持跳转定义、查找引用、自动重命名、自动导包和常用快速修复。
5. 支持 Git Changes、并排 Diff、暂存、撤销和提交。
6. 支持配置并运行 Maven、Gradle、npm、Python、Cargo、Docker 等项目命令。
7. 支持终端、日志、测试结果和错误位置跳转。
8. 为断点调试、线程、调用栈和变量查看预留统一接口。
9. 在相同项目和工作流下，内存占用明显低于完整 IntelliJ IDEA。

### 2.2 暂不实现

- 内置 AI、Codex 或其他模型调用
- Agent 任务面板、聊天和审批系统
- IDEA Ultimate 专有功能
- 数据库客户端和远程开发
- 插件市场和复杂插件生态
- 第一阶段的全量企业框架分析

## 3. 目标用户与典型流程

### 3.1 目标用户

- 使用 Java/Kotlin、Node.js、Python、Rust、Go 等语言的开发者
- 使用外部 AI 编程工具修改项目的开发者
- 需要高质量 Git、Diff、运行和调试体验的个人开发者
- 不希望 IDE 长期占用大量内存的用户

### 3.2 核心使用流程

```text
打开项目
   ↓
浏览文件与代码
   ↓
外部工具修改文件
   ↓
Lithe-IDEA 自动刷新并显示 Git Changes
   ↓
查看并排 Diff，逐块审查改动
   ↓
运行项目、查看日志和测试结果
   ↓
必要时进入 Debugger
   ↓
接受修改、撤销修改或提交 Git
```

## 4. 初版功能范围（MVP）

### 4.1 工作台与项目

- 打开本地目录作为项目
- 左侧 Project 文件树
- 文件创建、删除、重命名和刷新
- 中央编辑器、多标签页和分屏
- 外部文件变化自动检测
- 最近打开的项目
- 深色和浅色主题
- 工具窗口的显示、隐藏、折叠和尺寸保存

目标布局：

```text
┌─────────────────────────────────────────────────────────┐
│ 菜单 / 项目 / Git分支       Run Configuration    ▶  🐞 │
├──────────────┬──────────────────────────┬───────────────┤
│ Project      │ Editor / Diff            │ 可选工具窗口  │
│ 文件与模块树 │ 代码、改动与历史         │               │
├──────────────┴──────────────────────────┴───────────────┤
│ Run │ Debug │ Git │ Terminal │ Problems │ Test          │
└─────────────────────────────────────────────────────────┘
```

### 4.2 编辑器和代码理解

第一阶段优先支持 Java，同时保留其他语言的扩展接口。

- Tree-sitter 语法高亮
- 括号匹配、代码折叠和当前文件结构
- LSP 诊断和语义高亮
- 代码补全和参数提示
- 跳转到定义
- 查找所有引用
- 类型和文档悬浮提示
- 自动重命名
- 自动导包和整理导入
- 快速修复和常用代码操作
- 基础格式化

Java 使用 Eclipse JDT Language Server；其他语言通过对应的 LSP 服务接入。

### 4.3 Git 与 Diff

- 当前分支和仓库状态
- 修改、新增、删除文件列表
- 未暂存区与暂存区
- 文件级和代码块级 Diff
- 并排 Diff
- 单块接受、撤销和暂存
- 文件暂存和取消暂存
- 提交信息和 Commit
- 外部修改自动刷新
- 丢弃修改前确认

第二阶段增加 Git Log、分支图、分支切换和三方冲突解决。

### 4.4 Run Configuration 与项目运行

运行功能调用项目自己的工具链，不在 IDE 内重写 Maven、Gradle 或 npm。

每个运行配置包含：

- 名称
- 命令
- 参数
- 工作目录
- 环境变量
- 前置任务
- 是否使用终端

示例：

```json
{
  "name": "Backend",
  "type": "shell",
  "command": "./gradlew",
  "args": ["bootRun"],
  "workingDirectory": "${workspace}/backend",
  "environment": {
    "SPRING_PROFILES_ACTIVE": "dev"
  },
  "beforeRun": ["saveAll"]
}
```

MVP 支持任意 Shell 命令，并提供以下常用命令的友好配置：

```text
./gradlew bootRun
./mvnw spring-boot:run
npm run dev
python main.py
cargo run
docker compose up
```

运行面板需要支持启动、停止、重启、日志搜索、日志清空、退出码和运行时间。

### 4.5 终端与问题面板

- 项目目录终端
- 多终端标签
- 复制、粘贴和搜索
- 终端尺寸调整
- 运行日志输出
- 错误路径和行号解析
- 点击错误跳转到代码
- Problems 面板汇总 LSP 与构建诊断

### 4.6 Debugger 接口

MVP 先完成进程生命周期和调试配置的数据模型，第二阶段完成完整 DAP 页面：

- 断点
- 继续、暂停和停止
- 单步进入、跳过和跳出
- 线程
- 调用栈
- 局部变量和 Watch
- Debug Console

不同语言通过 Debug Adapter Protocol 适配，不把语言差异写入工作台 UI。

## 5. 技术架构

### 5.1 总体原则

主程序负责 UI 和协调；代码智能、构建、运行、Git 和调试通过独立服务或外部进程提供。

```text
IDEA风格 Workbench UI
          ↓
      应用协调层
   ┌──────┼──────┬──────┬──────┐
 工作区   Git   Runner 语言   Debug
   │       │      │       │      │
 文件系统 系统Git PTY   LSP    DAP
```

### 5.2 底座候选

当前目录包含两个开源底座，用于技术评估：

- `zed-main`：轻量、Rust 原生、性能潜力较好，作为长期产品底座候选。
- `intellij-community-master`：用于研究 IDEA 的工作台、Git、Diff、代码分析和 Debug 交互，不默认作为最终运行时底座。

第一阶段先分别完成构建可行性和内存基线，再确定是否以 Zed 为产品底座。

### 5.3 模块划分

```text
workbench/       IDEA风格布局、工具窗口和命令
workspace/       项目、文件监听和工作区状态
editor/          编辑器、标签页和编辑操作
language/        Tree-sitter、LSP管理和代码导航
git/             状态、Diff、暂存、提交和分支
runner/          Run Configuration、PTY和进程树
debugger/        DAP、断点、调用栈和变量
search/          文件搜索和文本搜索
settings/        全局、工作区和临时配置
platform/        文件系统、进程、日志和系统适配
```

### 5.4 代码智能生命周期

```text
只浏览文件       → 不启动项目语言服务器
打开 Java 项目   → 按需启动 JDT LS
持续编辑         → 通过 LSP 获取诊断和代码智能
关闭项目         → 终止或回收语言服务器
语言服务异常     → 独立重启，不拖垮主界面
```

Tree-sitter 提供即时语法能力，LSP 提供跨文件和跨模块的类型理解。这样可以保留跳转、引用、重命名、类型推断、语法检查和自动导包，同时避免主程序常驻完整 PSI 平台。

## 6. 性能与内存目标

性能目标必须通过同一台机器、同一项目和同一操作流程进行实测。

初步目标：

| 场景 | 目标 |
|---|---:|
| 空窗口 | 150 MB 以内 |
| 普通项目、不启动 LSP | 300 MB 以内 |
| Java 项目 + JDT LS | 1 GB 以内 |
| 运行中的项目进程 | 与 IDE 分开统计 |
| 关闭项目后 | 主程序回落到基础占用附近 |

优化策略：

- 不维护完整 IntelliJ PSI
- LSP 按项目和语言按需启动
- 排除 `node_modules`、`build`、`target`、`.git` 等目录
- Git Log、Diff 和日志采用分页或上限缓冲
- 关闭项目时释放语言服务和子进程
- 外部进程崩溃不影响主 UI
- 不加载不需要的语言、插件和后台服务

## 7. 分阶段计划

### Phase 0：底座评估

- 编译并启动 Zed 与 IntelliJ Community 开源部分
- 记录空窗口、打开项目、索引和运行项目时的内存
- 评估工作台布局改造成本
- 记录许可证、商标和发行限制
- 形成底座选型结论

### Phase 1：IDEA UI 参考与页面空壳（当前阶段）

- 阅读 IntelliJ UI 源码，建立 Project、Editor、ToolWindow、Run/Debug 的页面参考
- 用独立原型准确还原 IDEA 的布局层级、间距、工具栏、标签页和底部面板
- 只保留首版需要的 Project、Editor、Git、Run、Terminal、Problems 和 Debug 入口
- 不在当前阶段构建 IntelliJ Platform，不下载 Android 外部模块
- 先确认页面结构，再选择轻量 UI 底座和真实能力接入方式

`intellij-community-master/` 是 UI 参考源码，`prototype/` 是页面实现目录。两者职责分开。

### Phase 2：真实工作区

- 项目目录读取和文件监听
- 编辑器真实文件读写
- 基础搜索
- 主题和布局持久化

### Phase 3：运行与 Git 闭环

- Run Configuration
- 进程管理和终端
- Git Changes
- 并排 Diff
- 暂存与提交
- Problems 面板

### Phase 4：代码智能

- Tree-sitter
- LSP 生命周期管理
- Java/JDT LS
- 诊断、补全、跳转和引用
- 重命名、自动导包和快速修复

### Phase 5：调试与工程体验

- DAP 客户端
- 断点和调用栈
- 变量树与 Debug Console
- Git Log 和分支图
- 冲突解决
- 测试结果树

## 8. MVP 验收标准

使用一个普通 Maven/Gradle Java 项目完成以下流程：

1. 打开项目并在 Project 面板浏览文件。
2. 打开 Java 文件，看到语法高亮和错误提示。
3. 跳转到类或方法定义。
4. 查找一个方法的全部引用。
5. 自动重命名一个方法并预览修改。
6. 外部程序修改文件后，编辑器和 Git 状态自动刷新。
7. 查看并排 Diff，并暂存其中一个代码块。
8. 点击运行按钮启动项目并在 Run 面板查看日志。
9. 停止并重启项目。
10. 提交修改并看到新的 Git 状态。
11. 关闭项目后，语言服务和运行子进程可以被正确回收。

## 9. 非目标与边界

Lithe-IDEA 不是：

- IntelliJ IDEA 的闭源 Ultimate 替代品
- AI 编程助手或模型客户端
- 通过复制 JetBrains 品牌资产获得的产品
- 一次性支持所有语言和所有企业框架的 IDE

项目可以参考 IDEA 的布局、信息架构和交互习惯，但使用自己的产品名称、图标、主题资源和实现代码，并遵守所采用开源底座及依赖的许可证要求。

## 10. 当前仓库说明

```text
Lithe-IDEA/
├── 需求开发书.md
├── intellij-community-master/   # UI 参考源码
├── zed-main/                    # 轻量底座候选
└── prototype/                   # Lithe-IDEA 页面原型
```

需求开发书是 Lithe-IDEA 项目层文档，不属于任何一个上游仓库。后续正式开发代码应放在新的产品目录或明确的产品分支中，避免直接污染上游源码。
