# Lithe 需求开发书

## 1. 产品定义

- 产品名：Lithe
- 中文名：轻舟
- 平台：仅 macOS
- 技术路线：Swift 6、SwiftUI 与 AppKit
- 产品定位：面向 AI 编程时代的本地代码浏览、外部变更观察和 Git 审查工作台

> AI 负责产生和运行代码，Lithe 负责人类阅读、理解并决定是否接受这些改动。

Lithe 借鉴 IntelliJ IDEA 成熟的工作台信息架构和操作习惯，但使用自己的品牌、视觉令牌和原生 macOS 实现。它不是 IntelliJ IDEA 的复制品，也不尝试重新实现一套完整传统 IDE。

## 2. 产品背景

外部 AI 编程工具已经能够完成大量代码生成、修改、运行和测试工作。用户仍需要一个可靠、轻量的本地界面来：

- 打开项目并快速浏览目录和代码
- 及时看到外部工具修改了哪些文件
- 处理外部版本和未保存内容之间的冲突
- 通过并排 Diff 审查每一处改动
- 对修改执行暂存、撤销和提交
- 必要时做少量人工修正

Lithe 不调用 AI 模型，不承载 Agent 会话、聊天或审批系统。它观察同一个本地项目目录，与外部 AI 工具协作。

## 3. 设计原则

1. **审查优先**：Changes 和 Diff 与 Project、Editor 同等重要。
2. **外部变化优先**：文件监听和冲突处理属于基础能力，不是附加功能。
3. **原生 macOS**：使用系统窗口、菜单、快捷键、文件面板和 SF Symbols。
4. **轻量按需**：不常驻 JVM、Chromium、语言服务器或构建进程。
5. **真实交互**：可见按钮必须真实工作；未接入能力明确显示为空状态。
6. **可恢复开发**：项目使用本地 Git 管理，并按独立功能分段提交。

## 4. 核心用户流程

```text
打开本地项目
    ↓
浏览文件和代码
    ↓
外部 AI 修改项目
    ↓
Lithe 自动发现文件与 Git 状态变化
    ↓
查看修改文件和并排 Diff
    ↓
少量人工修正（可选）
    ↓
暂存、撤销或提交
```

## 5. MVP 功能范围

### 5.1 Welcome Screen

- IDEA 风格的启动页空间结构
- 最近项目列表
- 搜索最近项目
- 打开本地目录
- 识别并弱化已失效路径
- 从最近项目中移除记录

### 5.2 工作台

- 顶部项目与当前 Git 分支信息
- 左侧 Project、Changes、Search 活动栏
- 左侧工具面板
- 中央 Editor / Diff 区域
- 底部轻量状态栏
- IDEA 用户熟悉的高信息密度
- Lithe 自己的色彩、图标和产品文案

### 5.3 Project 与文件

- 打开本地目录
- 文件树展开与折叠
- 目录优先排序
- Finder 中显示文件
- 复制文件路径
- 默认排除 `.git`、`.build`、`node_modules`、`target`、`build`、`dist` 等目录
- 使用 FSEvents 监听整个项目层级
- 外部新增、修改或删除文件后刷新工作区

### 5.4 编辑器

- 多标签浏览
- 原生 AppKit 文本编辑
- 行号、选择、滚动和 Undo
- 保存与未保存状态
- 关闭脏文件前确认
- 当前文件查找由系统文本能力提供
- 基础语法着色
- 路径面包屑

编辑器用于阅读和少量修正，不以大量人工编码为主要目标。Tree-sitter 可以在后续替换第一版内置着色器，但不接入 LSP、自动补全或重构系统。

### 5.5 搜索

- 按文件名和相对路径搜索
- 项目全文搜索
- 显示文件、行号和匹配预览
- 点击结果打开文件
- 默认跳过生成目录和超大文件

### 5.6 外部变更与冲突

- 通过 FSEvents 监听外部修改
- 未保存内容没有冲突时自动加载磁盘版本
- 同时存在外部修改和未保存内容时显示冲突提示
- 用户可以选择加载磁盘版本或保留编辑器版本
- 外部变化触发文件树与 Git Changes 刷新

### 5.7 Git 与 Diff

- 使用系统 `/usr/bin/git`
- 当前仓库和分支
- 修改、新增、删除文件列表
- 暂存区与工作区分组
- 并排 Diff
- 文件级暂存与取消暂存
- 丢弃修改前确认
- 删除未跟踪文件前确认
- 输入提交信息并提交暂存内容
- Git 状态自动刷新

代码块级暂存和撤销是 MVP 后续增量，第一版先保证文件级操作稳定。

### 5.8 Run 空壳

- 保留顶部 Run 入口以验证布局
- 点击后明确提示尚未接入
- 不模拟虚假的运行成功状态
- 项目运行继续由外部 AI 工具或终端负责

## 6. 当前明确不开发

- AI、Codex 或其他模型调用
- Agent 会话和聊天界面
- LSP、代码补全、参数提示和悬浮文档
- 自动导包、快速修复和安全重命名
- 完整代码格式化和项目级代码分析
- Run Configuration 和进程管理
- 内置终端和 PTY
- 测试识别、测试树和覆盖率
- Debugger、DAP、断点和变量查看
- Git Log、分支图和三方冲突解决
- Local History、TODO、书签和多根工作区
- 插件市场、数据库客户端和远程开发
- 浅色主题和复杂设置系统

这些能力未来只有在真实用户需求出现后才重新评估，不作为当前路线的默认阶段。

## 7. 技术架构

```text
SwiftUI Workbench
├── Welcome / Recent Projects
├── Project / Changes / Search
├── Editor Tabs / Diff Review
└── Status / Native Commands
          ↓
AppKit NSTextView     FSEvents      System Git
原生文本编辑          外部变化      状态与审查操作
```

代码模块：

```text
Sources/Lithe/
├── Models/       工作区、文档、Git 与界面状态
├── Services/     文件扫描、最近项目、FSEvents、Git
├── Theme/        Lithe 视觉令牌
└── Views/        Welcome、Workbench、Editor、Diff 等界面
```

项目使用 Swift Package Manager 构建，最低支持 macOS 14。

## 8. 性能目标

| 场景 | 目标 |
|---|---:|
| Welcome 空窗口 | 150 MB 以内 |
| 普通项目 | 300 MB 以内 |
| 关闭项目后 | 回落到基础占用附近 |

实施原则：

- 不维护 IntelliJ PSI 或完整项目索引
- 不启动语言服务器、终端和构建进程
- 文件扫描跳过常见生成目录
- 搜索限制单文件大小和结果数量
- Diff 不一次性加载无限历史
- 关闭项目时停止文件监听并释放工作区状态

## 9. 开发阶段

### Phase 1：原生可用闭环（当前）

- Welcome Screen
- IDEA 风格工作台
- 文件树、多标签和基础编辑
- 最近项目和项目搜索
- FSEvents 外部变化监听
- Git Changes、并排 Diff、文件级暂存/撤销/提交

### Phase 2：审查体验打磨

- 代码块级暂存与撤销
- Diff 内搜索和跳转
- 更准确的语法解析与大文件策略
- 标签、面板尺寸和打开状态持久化
- 文件创建、重命名和安全删除
- 可访问性、性能和异常恢复

### Future：按需求重新评估

- Run、Terminal、Test、Debugger
- LSP 与代码智能
- Git 历史、分支和冲突解决

## 10. MVP 验收标准

1. 启动 App 并从 Welcome Screen 打开本地 Git 项目。
2. 在 Project 面板浏览目录并打开多个文本文件。
3. 编辑并保存文件，关闭未保存标签时得到确认。
4. 搜索文件名或文本并从结果打开文件。
5. 外部程序修改文件后，文件树和 Changes 自动刷新。
6. 外部程序修改当前脏文件时，用户可以选择磁盘版或编辑器版。
7. 在 Changes 中查看未暂存和已暂存文件。
8. 查看文本文件的并排 Diff。
9. 暂存、取消暂存或确认后丢弃文件修改。
10. 输入提交信息并提交暂存内容。
11. 关闭项目后回到 Welcome Screen，最近项目仍然存在。

## 11. 当前仓库

```text
Lithe-IDEA/
├── Package.swift
├── Sources/Lithe/          # 原生 macOS App
├── prototype/              # 早期 HTML 视觉参考
├── 需求开发书.md
└── IDEA-UI源码参考.md
```

仓库由本地 Git 管理。每个可独立恢复的功能阶段创建单独提交。
