[package]
name = "lithe-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Cross-platform application core for Lithe"

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
regex = "1.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
